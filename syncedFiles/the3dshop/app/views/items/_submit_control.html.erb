<% if current_user != @item.user %>
	<%= form_for @order_item, remote: true do |f| %>
	  <%= f.hidden_field :item_id, value: @item.id %>
	  <%= f.submit "Add to Cart !", class: "btn btn-primary btn_submit" %>
	<% end %>
<% end %>