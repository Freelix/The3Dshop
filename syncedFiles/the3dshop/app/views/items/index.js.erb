$('#results').html("<%= escape_javascript(render :partial => 'results') %>");

$(document).on("ajax:complete", "#search_form", function() {
  $("#spinner").fadeOut('fast');
});

/*
  ajax:before
  ajax:loading
  ajax:success
  ajax:complete
  ajax:failure
  ajax:after
*/